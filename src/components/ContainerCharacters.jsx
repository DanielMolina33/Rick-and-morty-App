import React from 'react';

import '../assets/styles/components/ContainerCharacters.css';

const ContainerCharacters = ({children}) => (
  <main className="mainContainer">
    {children}
  </main>
);

export default ContainerCharacters;